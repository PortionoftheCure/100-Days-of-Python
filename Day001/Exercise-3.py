# Objective is to count characters in a given name
# googled python function for counting string, len is the function
#len()

print(len(input("What is your name? ")))

# alterntatively (course has not taught setting variables yet)

#string = input("What is your name?")
#print(len(string))
